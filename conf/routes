# Message Controller
GET         /home                controllers.TemplateController.getHomepage
POST        /home                controllers.TemplateController.submitMessage

GET         /canto-home          controllers.HomeController.showHomepage

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file        controllers.Assets.versioned(path="/public", file: Asset)